/**
 * No import is required since we have declared the type in the ambirent module.
 * And also we are loading both app.ts and util.js files in HTML together.
 */
const formattedDate = formatDate(new Date())
console.log(formattedDate)